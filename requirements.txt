# Active-Speaker Detection and Smart Video Reframing
# Baseline + active-speaker detection

# --- Baseline: video analysis, face tracking, reframing ---
opencv-python>=4.8.0
mediapipe>=0.10.9
numpy>=1.24.0
PyYAML>=6.0
pytest>=7.0

# --- Active-speaker detection: audio extraction and Silero voice activity detection ---
# CPU-only PyTorch. Install the CPU wheel explicitly to avoid CUDA packages:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
silero-vad==6.2.1
soundfile>=0.12.0
onnxruntime>=1.17.0