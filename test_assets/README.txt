This folder is intentionally empty in the submission package.

Drop test video files (.mp4) here before running. The integration tests
discover assets in this directory at runtime and skip gracefully when an
asset is absent, so only the files you place here will be exercised.
